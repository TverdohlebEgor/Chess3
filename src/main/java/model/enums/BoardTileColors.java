package model.enums;

import java.awt.*;

public record BoardTileColors() {
    public static final Color lightSquare = new Color(240, 217, 181);
    public static final Color darkSquare = new Color(181, 136, 99);
}
